import { Button } from 'lib';
import styled from 'styled-components';

export const HeaderFunctionalitiesStyled = styled.div`
    position: relative;
    display: flex;
    flex-direction: row;
    justify-content: flex-end;
    align-items: center;
    gap: 8px;
`;

export const HeaderFunctionality = styled(Button)``;